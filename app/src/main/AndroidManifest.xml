<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dudi.myidea"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission-sdk-23
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="23" />

    <!-- uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE" / !-->

    <uses-permission-sdk-23
        android:name="android.permission.CAMERA"
        android:maxSdkVersion="23" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyCompositionsListActivity" />
        <activity android:name=".LayoutListActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".SaveCompositionActivity" />
        <activity android:name=".ImageViewerActivity" />
        <activity android:name=".LayoutForThreeActivity" />
        <activity android:name=".LayoutForFourActivity" />
        <activity android:name=".LayoutForFiveActivity" />
        <activity android:name=".LayoutForSixActivity" />
        <activity android:name=".LayoutForSevenActivity" />
        <activity android:name=".LayoutForEightActivity" />

    </application>

</manifest>
